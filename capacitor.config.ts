import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sofiia.moroz',
  appName: 'whether-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
